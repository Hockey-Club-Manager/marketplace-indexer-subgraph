type Token @entity {
  id: ID!
  title: String!
  media: String!
  extra: String!
  issued_at: BigInt!
  tokenId: String!
  owner: User!
  ownerId: String!
  perpetual_royalties: String!
}

type User @entity {
  id: ID!
  tokens: [String!]
}